#include "parser.h"



char    *ft_parse_input(int argc, char **argv)
{
    if (argc != 5)
    {
        printf("Not the right number of arguments\n");
        return (NULL);
    }
    return ("Something");
    //rt = join(argv[1]..argv[4])
}
